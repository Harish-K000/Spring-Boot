<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

  <modelVersion>4.0.0</modelVersion>

  <groupId>com.example.platform</groupId>
  <artifactId>backend-platform</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <packaging>pom</packaging>

  <name>backend-platform</name>
  <description>Production Grade Backend Platform (Monorepo Parent)</description>

  <properties>
    <java.version>25</java.version>
    <!-- This parent is not spring-boot-starter-parent, so java.version alone is inert:
         maven.compiler.release is what actually drives javac. -->
    <maven.compiler.release>${java.version}</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <spring-boot.version>3.5.10</spring-boot.version>
    <spring-cloud.version>2025.0.0</spring-cloud.version>
    <lombok.version>1.18.48</lombok.version>
    <maven-compiler-plugin.version>3.13.0</maven-compiler-plugin.version>
  </properties>

  <!-- Centralized dependency versions for all modules (services/libs) -->
  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring-boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>org.springframework.cloud</groupId>
        <artifactId>spring-cloud-dependencies</artifactId>
        <version>${spring-cloud.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <!-- Inherited by every module: Lombok for entities, H2 for slice tests. -->
  <dependencies>
    <dependency>
      <groupId>org.projectlombok</groupId>
      <artifactId>lombok</artifactId>
      <version>${lombok.version}</version>
      <optional>true</optional>
    </dependency>
    <dependency>
      <groupId>com.h2database</groupId>
      <artifactId>h2</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-aop</artifactId>
    </dependency>
    <dependency>
      <groupId>io.micrometer</groupId>
      <artifactId>micrometer-registry-prometheus</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>io.micrometer</groupId>
      <artifactId>micrometer-tracing-bridge-otel</artifactId>
    </dependency>
    <dependency>
      <groupId>io.opentelemetry</groupId>
      <artifactId>opentelemetry-exporter-otlp</artifactId>
      <scope>runtime</scope>
    </dependency>
  </dependencies>

  <modules>
    <module>libs/common-observability</module>
    <module>libs/common-security</module>
    <module>services/auth-service</module>
    <module>services/orders-service</module>
    <module>services/inventory-service</module>
    <module>services/payments-service</module>
    <module>services/edge-gateway</module>
    <module>services/worker</module>
    <module>services/engineering-agent</module>

    <!-- Optional shared libs -->
    <!-- <module>libs/common-web</module> -->
  </modules>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.springframework.boot</groupId>
          <artifactId>spring-boot-maven-plugin</artifactId>
          <version>${spring-boot.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>${maven-compiler-plugin.version}</version>
          <configuration>
            <!-- Retains parameter names in the bytecode. spring-boot-starter-parent normally sets
                 this; without it Spring MVC cannot infer @PathVariable/@RequestParam names and
                 every such handler fails at runtime. -->
            <parameters>true</parameters>
            <!-- JDK 23+ no longer runs discovered annotation processors implicitly. The entities
                 use Lombok, so Java 25 builds must opt in to annotation processing. -->
            <proc>full</proc>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>

</project>
